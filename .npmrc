# @sentry/nextjs@9 declares peer support only up to Next 15, but this project
# runs Next 16. The integration works at runtime; this allows install to proceed
# until Sentry ships a release with a Next 16 peer range. Remove when upgrading
# @sentry/nextjs to a version that lists next@^16 as a supported peer.
legacy-peer-deps=true
